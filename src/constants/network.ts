/** Local-network ports used by LocalLink's serverless peer flow. */

/** TCP port used to find LocalLink peers on the same Wi-Fi/LAN. */
export const DISCOVERY_PORT = 8786;

/** TCP port used for serverless WebRTC signaling after a peer is discovered. */
export const SIGNAL_PORT = 8787;
