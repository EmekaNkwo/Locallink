/** BLE + signaling configuration (PRD F1). */

/** Service UUID every LocalLink device advertises + scans for. */
export const LOCALLINK_SERVICE_UUID = '8d3a0001-7f9c-4b21-9d6e-2a1f5c0b7e10';

/** Local-network port used for serverless WebRTC signaling (same-Wi-Fi assumption). */
export const SIGNAL_PORT = 8787;
